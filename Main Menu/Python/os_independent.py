import os

main_dir = os.path.split(os.path.abspath(__file__))[0]
data_dir = os.path.join(main_dir, "data")
fname = "gg"
local_dir = (os.path.join(dmain, fname))
print(main_dir)
print(data_dir)
print(local_dir)